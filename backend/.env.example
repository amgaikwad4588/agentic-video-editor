# Copy to .env and fill in. Everything has a working default except the key.

# Required only for the agent chat; the rest of the editor works without it.
ANTHROPIC_API_KEY=

# Optional overrides
# AGENT_MODEL=claude-opus-4-8
# AGENT_MAX_ITERATIONS=12
# DATA_DIR=./data
# FFMPEG_PATH=
# FONT_PATH=
# MAX_UPLOAD_MB=1024
